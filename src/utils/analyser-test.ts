// Runs the analyse on an input. Usage: `node analyse-test.ts programName` for pre written programs in test-programs.ts
// `node analyse-test.ts file/string fileName/stringName` to read strings from a file or from the command line respectively

import { analyserTest } from './test-utils'

analyserTest()
